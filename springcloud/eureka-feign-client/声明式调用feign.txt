1.	在启动类上添加@EnableFeignClients注解开启Feign Client的功能
2.	在Feign Client接口上加@FeignClient注解来声明一个Feign Client,其中value指定远程
	调用的其他服务的服务名
	
			/**
		 *   @author Administrator
		 *  	 在接口上加@FeignClient注解来声明一个Feign Client
		 *  	value为远程调用其他服务的服务名
		 *  	FeignConfig.class为Feign Client的配置类,可以覆盖默认配置
		 *
		 */
		@FeignClient(value="eureka-client",configuration=FeignConfig.class)
		public interface EurekaClientFeign {
			@GetMapping(value="/hi")
			String sayHiFromClientEureka(@RequestParam(value="name") String name);
		
		}
	 	
	 	
Feign的实现原理:
	1)	首先通过@EnableFeignClients注解开启FeignClient的功能。只有这个注解存在,才会在程序启动
		时开启对@FeignClient注解的包扫描
	2)	根据Feign的规则实现接口,并在接口上面添加@FeignClient注解
	3)	程序启动后,会进行包扫描,扫描